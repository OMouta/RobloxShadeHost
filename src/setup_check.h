#pragma once

// Logs what is installed beside the exe and what looks wrong: ReShade, its effects, the GPU the host runs
// on, and the optional add-ons. Nothing here stops the host from starting.
void CheckSetup();
